# LazyAZ Environment Variables

# Azure DevOps Organization URL (e.g., https://dev.azure.com/your-organization)
# This can also be configured via Azure CLI: az devops configure --defaults organization=https://dev.azure.com/your-organization
AZURE_DEVOPS_ORG=https://dev.azure.com/your-organization

# Authentication is handled via Azure CLI
# Login to Azure first using: az login
# Then install the DevOps extension if needed: az extension add --name azure-devops

# Optional: Azure DevOps API Version (defaults to 6.0 if not specified)
# Optional: Default Azure DevOps Project 
# This can also be configured via Azure CLI: az devops configure --defaults project=your-project-name
# AZURE_DEVOPS_PROJECT=your-project-name

# Optional: Log level (debug, info, warn, error)
# LOG_LEVEL=info


